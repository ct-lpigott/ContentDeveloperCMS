extends view_components/layout.pug

block main-body

    h1 Content Developer Admin
    h2 Welcome back #{userDisplayName}
    img(src=userProfileImage title=userDisplayName class="profileImage")

    form(method="POST" action="/feeds/"+userID)
        label Project Name
            input(type="text" name="projectName")
        input(type="submit" value="Create New Project")
    
    ul#userProjects

    script(src='/js/admin_script.js')